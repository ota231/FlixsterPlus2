package com.example.flixsterplus

/*
* used by the adapter to ensure it has an appropriate listener
* implemented by fragment
* */

interface OnListFragmentInteractionListener {
    fun onItemClick(item: Movie)
}